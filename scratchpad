SELECT
  T1."Month" || '-01' AS "Month",
  T1."FormattedDue",
  T1."FormattedResolution"
FROM T1

===

SELECT
  FORMATDATE(DATE(T1."Month" || '-01'), 'MMMM yyyy') AS "Month",
  T1."FormattedDue",
  T1."FormattedResolution"
FROM T1
