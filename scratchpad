import csv, time
from datetime import datetime

now = int(time.time())
with open('vulnonly.csv') as f:
    reader = csv.reader(f)
    for row in reader:
        sev = row[0].strip('"')
        dt = datetime.strptime(row[1].strip('"'), '%Y-%m-%dT%H:%M:%SZ')
        age = int((dt.timestamp() - now) / 86400)
        # apply logic and print
